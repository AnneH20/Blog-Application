<!DOCTYPE html>
<html>
<head>
    <title>Users</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .banner {
            background-color: #f4f4f4;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .banner-title {
            margin: 0;
            font-size: 24px;
        }
        .nav-tabs {
            display: flex;
            justify-content: space-between;
            background-color: #333;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .nav-tabs a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            font-size: 16px;
        }
        .nav-tabs a:hover {
            background-color: #555;
        }
        .nav-tabs .active {
            background-color: #007BFF;
        }
        .user-header {
            margin-bottom: 15px;
        }
        .username {
            font-size: 18px;
            font-weight: bold;
            color: #007BFF;
        }
        .user-meta {
            font-size: 14px;
            color: #555;
            margin-top: 5px;
        }
        .button-container {
            margin-bottom: 20px;
        }
        .button-container button {
            padding: 10px 15px;
            margin-right: 10px;
            border: none;
            border-radius: 5px;
            background-color: #007BFF;
            color: white;
            cursor: pointer;
            font-size: 14px;
        }
        .button-container button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container">
    {{#banner}}
        <section>
            <header class="banner">
                <h2 class="banner-title">{{title}}</h2>
                <div class="banner-content">{{content}}</div>
            </header>
        </section>
    {{/banner}}

    <div class="nav-tabs">
        <a href="/home">Home</a>
        <a href="/articles">Articles</a>
        <a href="/users" class="active">Users</a>
    </div>

    <h1>All Users</h1>
    <div class="button-container">
        <button onclick="window.location.href='/user/new';">Create New User</button>
    </div>
    <div class="users">
        {{#users}}
            <section>
                <header class="user-header">
                    <div class="username">{{username}}</div>
                    <div class="user-meta">{{firstName}} {{lastName}}</div>
                </header>
            </section>
        {{/users}}
    </div>
</div>
</body>
</html>
