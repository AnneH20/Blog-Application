<!DOCTYPE html>
<html>
<head>
    <title>Edit User</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .banner {
            background-color: #f4f4f4;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .banner-title {
            margin: 0;
            font-size: 24px;
        }
        .nav-tabs {
            display: flex;
            justify-content: space-between;
            background-color: #333;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .nav-tabs a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            font-size: 16px;
        }
        .nav-tabs a:hover {
            background-color: #555;
        }
        .nav-tabs .active {
            background-color: #007BFF;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        .form-group input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }
        .form-group button {
            padding: 10px 15px;
            border: none;
            background-color: #007BFF;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
        }
        .form-group button:hover {
            background-color: #0056b3;
        }
        .form-group .cancel-button {
            background-color: #6c757d;
            margin-left: 10px;
        }
        .form-group .cancel-button:hover {
            background-color: #5a6268;
        }
    </style>
</head>
<body>
<div class="container">
    <section>
        <header class="banner">
            <h2 class="banner-title">Edit User</h2>
        </header>
    </section>

    <div class="nav-tabs">
        <a href="/home">Home</a>
        <a href="/articles">Articles</a>
        <a href="/users" class="active">Users</a>
    </div>

    <h1>Edit User</h1>
    <form action="/user/{{username}}/edit" method="post">
        <div class="form-group">
            <label for="username">Username</label>
            <input type="text" id="updatedUsername" name="updatedUsername" value="{{user.username}}" required>
        </div>
        <div class="form-group">
            <label for="firstName">First Name</label>
            <input type="text" id="firstName" name="firstName" value="{{user.firstName}}" required>
        </div>
        <div class="form-group">
            <label for="lastName">Last Name</label>
            <input type="text" id="lastName" name="lastName" value="{{user.lastName}}" required>
        </div>
        <div class="form-group">
            <button type="submit">Update User</button>
            <button type="button" class="cancel-button" onclick="window.location.href='/users';">Cancel</button>
        </div>
    </form>
</div>
</body>
</html>
