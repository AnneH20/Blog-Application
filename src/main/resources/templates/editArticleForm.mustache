<!DOCTYPE html>
<html>
<head>
    <title>Update Article</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .banner {
            background-color: #f4f4f4;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .banner-title {
            margin: 0;
            font-size: 24px;
        }
        .nav-tabs {
            display: flex;
            justify-content: space-between;
            background-color: #333;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .nav-tabs a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            font-size: 16px;
        }
        .nav-tabs a:hover {
            background-color: #555;
        }
        .nav-tabs .active {
            background-color: #007BFF;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }
        .form-group textarea {
            height: 150px;
            resize: vertical;
        }
        .form-group button {
            padding: 10px 15px;
            border: none;
            background-color: #007BFF;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
        }
        .form-group button:hover {
            background-color: #0056b3;
        }
        .cancel-button {
            background-color: #ccc; /* Gray color for cancel button */
        }
        .cancel-button:hover {
            background-color: #aaa; /* Darker gray on hover */
        }
    </style>
</head>
<body>
<div class="container">
    <section>
        <header class="banner">
            <h2 class="banner-title">Edit Article</h2>
        </header>
    </section>

    <div class="nav-tabs">
        <a href="/home">Home</a>
        <a href="/articles">Articles</a>
        <a href="/users">Users</a>
    </div>

    <h1>Edit Article</h1>
    <form action="/article/{{slug}}/edit" method="post">
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="{{article.title}}" required>
        </div>
        <div class="form-group">
            <label for="content">Content</label>
            <textarea id="content" name="content" required>{{article.content}}</textarea>
        </div>
        <div class="form-group">
            <button type="submit">Update Article</button>
            <button type="button" class="cancel-button" onclick="window.location.href='/article/{{article.slug}}';">Cancel</button>
        </div>
    </form>
</div>
</body>
</html>
